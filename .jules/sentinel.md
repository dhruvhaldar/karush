## 2024-05-20 - Insecure PRNG in Randomized Rounding
**Vulnerability:** Predictable PRNG (`np.random.randn()`) was used in the `randomized_rounding` algorithm in `karush/convex/relaxations.py`.
**Learning:** Even mathematical and optimization algorithms must use secure PRNGs when deployed in contexts where predictability could be exploited (e.g., when the max-cut problem is applied to cryptography or network security). Using a global predictable PRNG can lead to deterministic outputs across sessions or predictable algorithmic behavior.
**Prevention:** Always use a securely seeded PRNG (e.g., `np.random.default_rng(secrets.randbits(128))`) for random decision-making in algorithms that may be applied to security-sensitive data or systems.
## 2024-05-24 - Secure PRNG Generation

**Vulnerability:** Predictable randomized rounding sequence due to using a cryptographically secure seed (`secrets.randbits`) with a non-cryptographic PRNG (`np.random.default_rng`).
**Learning:** Seeding a non-cryptographic PRNG with a secure seed does not make the generated sequence cryptographically secure. The internal state can still be predicted if enough outputs are observed.
**Prevention:** Use a true Cryptographically Secure PRNG (CSPRNG) like `secrets.SystemRandom()` directly for generating all random numbers when unpredictability is required.
## 2024-05-24 - Unsanitized Input to Solvers

**Vulnerability:** Core mathematical optimization functions lacked input validation for `NaN` and `Inf` values. Passing tainted data into solvers can cause unhandled exceptions during matrix operations or lead to infinite loops inside the line search mechanisms.
**Learning:** In scientific and optimization code, algorithms heavily rely on numeric stability. Unsanitized input propagates rapidly through matrix inversion and updates, acting as an application-level DoS vector or corrupting calculations silently.
**Prevention:** Validate input arrays at the entry point of all solver functions (e.g., using `np.isfinite()`) to ensure the problem data is well-formed before starting algorithmic loops.

## 2023-10-27 - [Configuration Validation for DoS Prevention]
**Vulnerability:** Missing validation for optimization parameters `tol` and `max_iter` could allow users to pass negative tolerances or extremely large/invalid iteration counts, leading to infinite loops or memory exhaustion (due to unbounded history appending).
**Learning:** In applied math libraries like `karush`, parameter validation is critical because the optimization loops depend on them for termination. Leaving them unchecked can result in silent data corruption or DoS vectors.
**Prevention:** Always validate configuration parameters such as `tol > 0` and `max_iter` as positive integers before starting optimization loops.

## 2024-05-20 - Missing bounds checks for tolerance and iterations
**Vulnerability:** DoS (Denial of Service) vulnerability due to missing bounds checking on `tol` (tolerance) and `max_iter` (maximum iterations) parameters in iterative optimization algorithms.
**Learning:** Mathematical solvers often use `tol` to evaluate convergence and `max_iter` to prevent infinite loops. If these inputs aren't sanitized, malicious users could provide a negative `tol` (making convergence impossible) or an enormous `max_iter`, causing the server to exhaust CPU resources in an infinite or extremely long-running loop.
**Prevention:** Always validate that `tol` is strictly positive (`tol > 0`) and `max_iter` is a reasonably bounded positive integer before entering iterative loops.

## 2024-05-24 - Missing bounds checks for integer parameters
**Vulnerability:** Application DoS vulnerability due to missing type and bounds checking on numeric parameters (like `num_trials`).
**Learning:** When iterative limits or sizes depend on user input, failure to validate the input type and bounds can lead to OOM crashes or unhandled exceptions.
**Prevention:** Always validate parameters (like `num_trials > 0`) before using them in algorithms.
## 2024-05-18 - Missing Validation for Initial Barrier Parameters
**Vulnerability:** Core functions `barrier_method` and `solve_sdp_barrier` accepted non-positive values (e.g., `<= 0`) for `mu0` and `initial_mu`, leading to potential division by zero and unhandled exceptions (Denial of Service).
**Learning:** In barrier and interior point algorithms in `karush`, the initial penalty parameter must strictly be positive (`> 0`). Missing validation allows unexpected program termination which is critical in robust mathematical optimization libraries.
**Prevention:** Always ensure initial barrier parameters (e.g., `mu0`, `initial_mu`) are validated to be strictly positive at the start of the functions.
## 2024-04-07 - Unbounded max_iter Limit in Optimization Algorithms
**Vulnerability:** Core optimization algorithms accepted arbitrarily large `max_iter` limits, leading to potential memory exhaustion (DoS) via unbounded tracking arrays (`history.append`).
**Learning:** In purely mathematical algorithms, iteration counters inherently act as termination criteria and scale resource usage (CPU/memory). We need to place sensible upper bounds on input sizes, not just bounds on types or signs.
**Prevention:** Always bound parameters that control iteration depths or matrix array pre-allocations/append limits to a mathematically and system-level safe maximum (e.g. `10000`) before entering `while` or `for` loops.
## 2024-04-08 - Unbounded Cache Memory Exhaustion (DoS)
**Vulnerability:** The module-level dictionary `_svec_cache` in `karush/semidefinite/interior_point.py` grows infinitely without any size bounds when storing `np.triu_indices` for different matrix sizes `n`.
**Learning:** Custom caching mechanisms without eviction policies (like a simple dictionary) in long-running processes can lead to memory exhaustion attacks if an attacker can control the cache key (the matrix size `n` in this case).
**Prevention:** Always use bounded caches, such as `functools.lru_cache(maxsize=...)`, for caching results based on potentially attacker-controlled inputs.
## 2024-05-24 - Validation Bypass via NaN in Numeric Parameters
**Vulnerability:** Numeric parameters like `tol`, `mu0`, and `initial_mu` were validated using simple inequalities (e.g., `tol <= 0`). If an attacker passes `NaN`, the inequality evaluates to `False`, bypassing the validation. This causes the loop termination conditions (e.g., `norm(g) < tol`) to always fail, forcing the algorithm to run for the maximum number of iterations.
**Learning:** `NaN` values break standard inequality checks (`<`, `<=`, `>`, `>=` all return `False`). Attackers can exploit this to bypass positive/bounds checks, leading to forced maximum work (resource exhaustion DoS).
**Prevention:** Always use `np.isnan()` or strict type checks `isinstance(val, (int, float))` alongside inequality checks when validating numeric configurations in security-sensitive or performance-critical loops.
## 2024-05-24 - PRNG Security vs Performance Trade-off
**Vulnerability:** Predictable PRNG sequence due to non-cryptographic generator.
**Learning:** A proposed security fix to replace `np.random.default_rng(secrets.randbits(128))` with `secrets.SystemRandom()` was rejected because pure Python loops in vectorized mathematical operations cause severe performance regressions ('security theater').
**Prevention:** Retain the securely seeded NumPy PRNG for performance, and document the trade-off explicitly with a security comment to prevent future confusion.
## 2024-05-24 - Unhandled Exception (AttributeError) from List Inputs
**Vulnerability:** Core functions like `solve_eq_qp` threw unhandled `AttributeError` exceptions when users passed standard Python lists (e.g., `A = [[1, 2]]`) instead of NumPy arrays, because the code directly accessed `A.size`.
**Learning:** In scientific computing libraries, it's common for users to pass simple lists. Relying on NumPy-specific attributes (`.size`, `.shape`) without first ensuring the input is a valid NumPy array creates an easily triggerable denial of service vector (unhandled exception crash) in the solver.
**Prevention:** Always explicitly convert array-like inputs to NumPy arrays using `np.asarray(..., dtype=float)` at the very beginning of the function before performing attribute checks or operations.
## 2024-05-24 - Unhandled Exception (AttributeError) from List Inputs
**Vulnerability:** Core functions threw unhandled `AttributeError` exceptions when users passed standard Python lists (e.g., `A = [[1, 2]]`) instead of NumPy arrays, because the code directly accessed NumPy-specific attributes like `.shape` or `.T`.
**Learning:** In scientific computing libraries, it's common for users to pass simple lists. Relying on NumPy-specific attributes without first ensuring the input is a valid NumPy array creates an easily triggerable denial of service vector (unhandled exception crash) in the solver. Framing standard API type error fixes as "Security Enhancements" is considered "security theater" in a local math library context.
**Prevention:** Always explicitly convert array-like inputs to NumPy arrays using `np.asarray(..., dtype=float)` at the very beginning of the function before performing attribute checks or operations. Label these fixes accurately as DoS prevention or input validation, and integrate test cases into the established test suite rather than leaving scratchpad scripts in the repo root.
## 2025-02-23 - DoS Prevention from Unhandled Python Lists
**Vulnerability:** Optimizers like SQP and Barrier crashed (`AttributeError` or `TypeError`) when user-provided objective or constraint functions returned standard Python lists instead of NumPy arrays.
**Learning:** Functions that interact mathematically with numpy arrays must sanitize their inputs and external function return values by explicitly converting them using `np.asarray(..., dtype=float)`. The vulnerability arose because mathematical operators on mixed lists and arrays can fail unexpectedly (e.g., list `>` int comparison).
**Prevention:** In local mathematical libraries, always cast array-like inputs and function return values to NumPy arrays via `np.asarray` before performing matrix operations, attribute checks (like `.ndim`), or inequalities.
## 2025-02-23 - Validation Bypass via Boolean Types in Numeric Parameters
**Vulnerability:** Numeric parameters like `tol` and `max_iter` were validated using `isinstance(val, (int, float))`. Since `bool` is a subclass of `int` in Python, passing `True` or `False` bypassed the validation check, evaluating silently to `1` or `0`, which caused incorrect bounds checks (e.g. `tol=False` became `tol=0`, causing infinite loops/DoS).
**Learning:** Python's type hierarchy treats booleans as integers. Strict numeric validation must explicitly reject `bool` types before checking for `int` or `float` to prevent unintended logic errors and security bypasses in mathematical bounds checking.
**Prevention:** Always use `isinstance(val, bool)` to explicitly reject booleans when validating numeric configuration parameters (e.g., `if isinstance(val, bool) or not isinstance(val, (int, float)): raise ValueError(...)`).
## 2025-02-23 - DoS via Truth Value Ambiguity in Line Search
**Vulnerability:** If user-provided objective functions `f(x)` return multi-element Python lists (or numpy arrays), comparing them to a scalar or broadcasting an expected decrease within a Python `while` loop (e.g. `f(x + alpha * p) > fx + ...`) causes a `ValueError: The truth value of an array with more than one element is ambiguous.` This causes the entire optimization routine to crash (Denial of Service).
**Learning:** Mathematical operations on user-provided output must be aggressively sanitized. Unhandled multi-element evaluations passed into Python-level boolean evaluations inherently cause exceptions.
**Prevention:** In iterative line searches, always wrap objective function outputs using `np.asarray` and explicitly evaluate the boolean condition using `np.all()` or `np.any()` before using it as a truth value in `while` loops.
## 2025-02-23 - DoS via Unhandled Exceptions from Dimensional Constraints
**Vulnerability:** Optimizers like the KKT quadratic programming solver `solve_eq_qp` threw unhandled `LinAlgError` or `ValueError` due to improperly dimensioned inputs from end-users, missing the required checks. Passing 1-dimensional or empty 1D matrices for strictly 2D arrays like `G` and `A` causes these native underlying solvers to crash the entire application logic.
**Learning:** For application-facing interfaces wrapper linear algebra calls (like KKT block matrix construction and KKT solver functions), it's not enough to check that array elements are finite or match a generic shape. A 1D array masquerading as a 2D matrix structure crashes the underlying `np.linalg.solve` routines unconditionally.
**Prevention:** In local mathematical libraries, explicitly enforce dimensional validation (`ndim == 2`) prior to array reshaping, indexing bounds matching, or passing variables directly to external solver backends.
## 2025-02-23 - DoS via Unhandled Exceptions from Dimensional Constraints in SDP
**Vulnerability:** The KKT interior point solver `solve_sdp_barrier` threw unhandled `IndexError` or `ValueError` due to improperly dimensioned inputs from end-users, missing the required checks. Passing 1-dimensional arrays for strictly 2D matrices like `C` and `A_list`, or failing to wrap scalar `b` properly, causes these native underlying indexing or linear solvers to crash the entire application logic.
**Learning:** For application-facing interfaces wrapper linear algebra calls (like advanced symmetric vectorization and KKT solver functions), it's not enough to check that array elements are finite or match a generic type. A 1D array masquerading as a 2D matrix structure crashes the underlying indexing logic (e.g., `A_stack[:, idx_i, idx_j]`) and `np.linalg.solve` routines unconditionally.
**Prevention:** In local mathematical libraries, explicitly enforce dimensional validation (`ndim == 2` for matrices, `ndim == 1` for vectors) prior to array reshaping, advanced indexing, or passing variables directly to external solver backends.
## 2024-05-24 - Missing Input Dimension Validation Causes Unhandled Exceptions
**Vulnerability:** Core functions like `solve_eq_qp` failed to validate the dimensions (`ndim`) of all input arrays (specifically `c` and `b`), although it validated them for finiteness. This led to unhandled `ValueError` crashes (e.g., "could not broadcast input array...") when passing incorrectly shaped arrays, such as a 2D array instead of a 1D vector. This can be weaponized for Denial of Service (DoS).
**Learning:** Only validating input arrays for finite numbers using `np.isfinite` is insufficient when the array shape must match strict expectations for linear algebra operations or array assignment.
**Prevention:** In local mathematical libraries, always validate input array dimensions explicitly (e.g., `ndim == 1` for vectors, `ndim == 2` for matrices) before performing matrix operations, advanced indexing, or array assignment to prevent unhandled exception DoS vulnerabilities.

## 2024-05-20 - Unhandled Exception DoS in Newton Method via Array Dimensions
**Vulnerability:** The `newton_method` in `karush/unconstrained/newton.py` accepted functions returning multi-dimensional arrays for gradients and 1D arrays for Hessians without dimension validation, passing them directly to `np.linalg.solve()`.
**Learning:** Checking input types for `isfinite()` isn't sufficient. NumPy routines like `np.linalg.solve` will raise a `LinAlgError` or `ValueError` (e.g., from `gufunc` signature mismatch) if arrays do not meet their structural assumptions (e.g., expecting a 2D matrix for `H` and 1D vector for `g`). These unhandled exceptions cause DoS crashes.
**Prevention:** Always explicitly validate array dimensions (e.g., `g.ndim == 1`, `H.ndim == 2`) before passing user-provided arrays to linear algebra routines like `np.linalg.solve` or performing advanced indexing.

## 2024-05-27 - DoS via Unhandled Exceptions from Dimensional Constraints in General Optimization
**Vulnerability:** Core functions in `karush/constrained/barrier.py`, `karush/constrained/sqp.py`, and `karush/unconstrained/quasi_newton.py` evaluated user-provided function outputs and blindly cast them to `float` arrays, assuming specific dimensions (1D vectors for gradients, 2D matrices for Hessians). When users passed mathematically valid but dimensionally incorrect lists or scalars (e.g. 1D hessians), it bypassed numerical finiteness validation and crashed the inner loop logic (e.g., `np.dot` broadcasting errors, `np.linalg.solve` dimension mismatches) leading to application Denial of Service via unhandled exceptions.
**Learning:** For application-facing interfaces wrapping native matrix and vector operations, simple `np.isfinite` validation isn't enough. Native linear algebra and boolean evaluations expect exact shapes and will throw uncatchable `ValueError` or `LinAlgError` during broadcasting if not explicitly validated beforehand.
**Prevention:** In local mathematical libraries, explicitly enforce dimensional validation (`ndim == 1` for gradients, `ndim == 2` for Hessians/Jacobians) immediately after extracting and wrapping the user-provided function evaluations, and raise a structured, actionable `ValueError` before performing matrix operations or advanced indexing.
## 2025-02-23 - DoS via Unhandled Exceptions from Dimensional Constraints in Gradient Functions
**Vulnerability:** Optimizers like `conjugate_gradient` and `bfgs_method` evaluated user-provided gradient functions and blindly passed them into numpy operations like `np.dot(g, g)`. When a user provided a function returning a multidimensional array (e.g. a 2D matrix), `np.dot` caused unhandled `TypeError` or `ValueError` crashes, leading to Denial of Service via unhandled exception.
**Learning:** For application-facing interfaces wrapping native matrix and vector operations, simple `np.isfinite` validation isn't enough. Native linear algebra evaluations expect exact shapes.
**Prevention:** In local mathematical libraries, explicitly enforce dimensional validation (`ndim == 1` for gradients, `ndim == 2` for Hessians/Jacobians) immediately after extracting and wrapping the user-provided function evaluations, and raise a structured, actionable `ValueError` before performing matrix operations.
## 2025-02-23 - DoS via Unhandled Exceptions from Strict Shape Compatibility in Matrices
**Vulnerability:** Core linear algebra solvers like `solve_eq_qp`, `primal_dual_qp`, and `solve_sdp_barrier` were vulnerable to unhandled `ValueError` crashes. They only validated the generic number of dimensions (e.g. `ndim == 2`) of input matrices without verifying strict geometric compatibility (e.g. `G` must be square, `b` must match the rows of `A`). If users passed dimensionally correct but incompatible arrays (like a 2x3 matrix to a 2x2 identity requirement), it bypassed initial checks and crashed the underlying solver.
**Learning:** Validating `ndim` is insufficient to prevent unhandled exceptions in matrix operations. When constructing complex block matrices like KKT systems, native numpy functions and `np.linalg.solve` strictly enforce geometric constraints. Incompatible shapes crash the application unconditionally.
**Prevention:** Always implement explicit, strict shape compatibility validation (e.g., `G.shape[1] == G.shape[0]`, `c.shape[0] == G.shape[0]`) on user-supplied matrix arrays before performing matrix operations, advanced indexing, or linear algebra operations.
## 2026-05-03 - [Missing Strict Geometric Shape Validation DoS]
**Vulnerability:** The mathematical library functions accepted user-provided matrices and arrays and passed them to `np.linalg.solve` after only verifying the dimensionality (`ndim == 2`). They did not strictly validate the geometric shapes (e.g., ensuring a matrix is square, or that matrix and vector dimensions align).
**Learning:** Checking that `H.ndim == 2` or `np.isfinite(H)` is insufficient to prevent unhandled exceptions. Providing a non-square matrix (like a `2x3` matrix) or a dimension-mismatched array to `np.linalg.solve` will crash the application with a `LinAlgError` or `ValueError`, enabling a Denial-of-Service attack.
**Prevention:** Always perform strict geometric shape validation (`A.shape[0] == A.shape[1]`, `A.shape[0] == b.shape[0]`) before calling linear algebra routines or advanced indexing operations on user-provided arrays.
## 2026-05-03 - [Missing Strict Geometric Shape Validation DoS in Barrier]
**Vulnerability:** The mathematical library function `barrier_method` accepted user-provided functions that return matrices and arrays, and passed them to `np.linalg.solve` after only verifying the dimensionality (`ndim == 2` or `ndim == 1`). They did not strictly validate the geometric shapes (e.g., ensuring the gradient matrix matches the objective variable, or that the hessian is a square matrix matching the variable size).
**Learning:** Checking that `H.ndim == 2` or `np.isfinite(H)` is insufficient to prevent unhandled exceptions. Providing a non-square matrix (like a `2x3` matrix) or a dimension-mismatched array to `np.linalg.solve` or broadcasted operations like `+` will crash the application with a `LinAlgError` or `ValueError` (e.g., `operands could not be broadcast together`), enabling a Denial-of-Service attack.
**Prevention:** Always perform strict geometric shape validation (e.g., `hess.shape[0] == hess.shape[1]`, `grad_f.shape[0] == x.shape[0]`) on the outputs of user-provided functions before calling linear algebra routines or advanced indexing operations.
## 2025-05-24 - Missing Strict Geometric Shape Validation DoS
**Vulnerability:** Several optimization methods (BFGS, CG, SQP) converted user-provided function outputs to NumPy arrays but failed to strictly validate geometric dimensions (e.g., ensuring gradient arrays match the initial point shape, or Hessians are square matrices matching the problem dimension) before using them in linear algebra operations or index lookups.
**Learning:** Validating input arrays for `ndim` and finite values alone is insufficient. If standard dimensions are mismatched, `np.dot` or linear solvers can throw unhandled exceptions (like `ValueError` or `LinAlgError`), leading to a Denial of Service (DoS) when fed pathological function return values.
**Prevention:** Always enforce strict mathematical shape constraints (`g.shape[0] == x.shape[0]`, `W.shape[0] == W.shape[1] == x.shape[0]`) immediately after array conversion and before any operations.
## 2025-05-24 - Missing Strict Geometric Shape Validation DoS in Newton Method
**Vulnerability:** The `newton_method` converted user-provided function outputs to NumPy arrays but failed to strictly validate geometric dimensions (e.g., ensuring gradient arrays match the initial point shape) before using them in linear algebra operations.
**Learning:** Validating input arrays for `ndim` and finite values alone is insufficient. If standard dimensions are mismatched, `np.dot` or linear solvers can throw unhandled exceptions (like `ValueError` or `LinAlgError`), leading to a Denial of Service (DoS) when fed pathological function return values.
**Prevention:** Always enforce strict mathematical shape constraints (`g.shape[0] == x.shape[0]`) immediately after array conversion and before any operations.
## 2024-05-28 - Missing Strict Geometric Shape Validation DoS in SDP/Relaxation functions
**Vulnerability:** The mathematical library functions `svec`, `smat` in `karush/semidefinite/interior_point.py` and `randomized_rounding` in `karush/convex/relaxations.py` accepted user-provided matrices/vectors and only verified their generic dimensionality (`ndim == 2` or `ndim == 1`). They lacked strict geometric shape validations (e.g. verifying a matrix is square, or a vector has exact expected length `n*(n+1)//2`).
**Learning:** Checking dimensionality (`ndim == 2`) is not enough to prevent unhandled exceptions. Supplying non-square matrices or vectors with incorrect lengths crashes the application via `IndexError` or `ValueError` due to indexing and linear algebra requirements.
**Prevention:** Always perform strict geometric shape validations (`M.shape[0] == M.shape[1]`, `v.shape[0] == n * (n + 1) // 2`) before carrying out linear algebra routines or advanced indexing on arrays.
## 2025-05-24 - Missing Strict Geometric Shape Validation DoS in SQP
**Vulnerability:** The `sqp_equality_constrained` method converted outputs of user-provided constraint functions (`h` and `grad_h`) to NumPy arrays but failed to strictly validate geometric dimensions (e.g., ensuring gradient arrays match the initial point shape and the lengths correspond) before using them in linear algebra operations.
**Learning:** Validating input arrays for `ndim` and finite values alone is insufficient. If standard dimensions are mismatched, `np.linalg.solve` or assignment to pre-allocated block matrices can throw unhandled exceptions (like `ValueError` or `LinAlgError`), leading to a Denial of Service (DoS) when fed pathological function return values.
**Prevention:** Always enforce strict mathematical shape constraints (`A.shape[1] == x.shape[0]`, `c_val.shape[0] == A.shape[0]`) immediately after array conversion and before inserting arrays into linear systems or KKT matrices.

## 2024-05-28 - DoS via Unhandled Exceptions from Missing Array Conversion in Linear Algebra Functions
**Vulnerability:** Core functions `svec` and `smat` in `karush/semidefinite/interior_point.py` verified the `.ndim` and `.shape` of input arrays but failed to explicitly coerce user inputs to NumPy arrays first. When users provided non-NumPy types like standard Python lists or scalars, expressions like `M.shape` or `v.ndim` threw unhandled `AttributeError` or `IndexError` exceptions.
**Learning:** Validating properties of objects in Python without verifying or explicitly casting their type first allows simple inputs to crash execution. NumPy properties like `.shape` and `.ndim` are not present on raw Python lists or scalars.
**Prevention:** Always forcefully cast or validate array inputs using `np.asarray(..., dtype=float)` *before* accessing `.shape` or `.ndim` properties to ensure input sanitization and prevent unhandled exception DoS vulnerabilities in array processing operations.
## 2024-05-24 - Missing Input Validation leading to Memory Exhaustion (OOM DoS) in Convex Relaxations
**Vulnerability:** The `max_cut_sdp_relaxation` function in `karush/convex/relaxations.py` accepted any 2D matrix (e.g., shape `(100000, 2)`) but immediately proceeded to allocate a list of $n$ matrices, each of size $n \times n$ (where $n$ is `W.shape[0]`). If an attacker supplied an array with a large first dimension but a tiny second dimension, it bypassed basic bounds but caused an immediate `MemoryError` when attempting to allocate the constraint list, creating a severe Denial of Service vulnerability.
**Learning:** Checking that `W.ndim == 2` and that the matrix contains finite numbers is completely insufficient. The actual dimensions of the array dictate the algorithm's memory usage and algorithmic complexity. A missing geometric check allows a minimal payload (a heavily non-square array with small size) to trigger massive internal memory allocations.
**Prevention:** Always validate geometric shape properties (e.g., `shape[0] == shape[1]` for square matrices) exactly matching mathematical assumptions *before* iterating or allocating internal data structures based on those dimensions.

## 2025-05-28 - DoS via Divide-by-Zero and NaN Propagation in Interior-Point Solvers
**Vulnerability:** The mathematical library function `primal_dual_qp` in `karush/constrained/primal_dual.py` verified that initial point vectors `x0` and `z0` contained finite values and correct dimensions, but failed to validate that they were strictly positive (`> 0`). When users provided valid arrays containing zeros or negative values, it caused silent divide-by-zero operations (`z / x`) leading to `NaN` and `Inf` propagation inside the KKT matrix. This caused unhandled `RuntimeWarning` and mathematical divergence, resulting in a Denial of Service.
**Learning:** In interior-point mathematical solvers (like primal-dual algorithms), initial state variables representing constraints (e.g., `x0`, `z0`) must be explicitly validated to be strictly positive (`> 0`) arrays. Validating only for finiteness or dimensions is insufficient and can lead to unhandled divide-by-zero exceptions or silent data corruption via `NaN` propagation.
**Prevention:** Always perform explicit positivity validation (`np.any(x0 <= 0)`) on initial state variables that are mathematically required to be strictly positive before using them in interior-point solver algorithms.
## 2025-02-23 - Silent Data Corruption from Improper Exception Handling
**Vulnerability:** Core linear algebra solvers like `solve_eq_qp` caught native exceptions (`np.linalg.LinAlgError`) raised by `np.linalg.solve` when provided with singular matrices, and silently returned arrays of zeros instead of propagating the error.
**Learning:** In mathematical solvers, attempting to 'fix' unhandled exception DoS vulnerabilities by silently catching native linear algebra exceptions (e.g., `np.linalg.LinAlgError` for singular matrices) and returning arbitrary fallback values like `np.zeros()` is extremely dangerous. It corrupts mathematical correctness by masking legitimate unsolvable states and silently corrupts data for downstream processes. Such exceptions must be allowed to propagate so the caller can mathematically handle the unsolvable state.
**Prevention:** Never catch `np.linalg.LinAlgError` just to return arbitrary zeros or fallback values. Allow native mathematical exceptions to propagate, or raise a structured mathematical exception that indicates the exact nature of the failure (e.g., "Singular KKT matrix").
## 2025-10-24 - [Silent Data Corruption from Improper Exception Handling in Solvers]
**Vulnerability:** Core mathematical solvers (SQP, Primal-Dual, Barrier, Interior-Point, Newton) caught native exceptions (`np.linalg.LinAlgError`) raised by `np.linalg.solve` or `np.linalg.inv` when provided with singular matrices, and silently broke the iteration loop to return the last valid iterate, masking the error.
**Learning:** Attempting to 'fix' unhandled exception DoS vulnerabilities by silently catching native linear algebra exceptions and terminating early or returning fallback values is extremely dangerous. It corrupts mathematical correctness by masking legitimate unsolvable states and silently corrupts data for downstream processes.
**Prevention:** Never catch `np.linalg.LinAlgError` just to return fallback values or silently terminate execution loops in solvers. Allow native mathematical exceptions to propagate so the caller can handle the unsolvable state.

## 2024-05-25 - Prevent DoS via Unhandled Domain Exceptions in Mathematical Algorithms
**Vulnerability:** Initial points passed to mathematical algorithms (like `x0` in Log-Barrier and `X0` in SDP) were not explicitly validated for mathematical domain constraints (strict feasibility `g(x) < 0` and strict positive definiteness `X0 > 0`). This bypassed numerical validation, causing algorithms to crash deep inside the loops with unhandled native exceptions (e.g., `LinAlgError` during `np.linalg.inv(X)` or infinite loops) causing Denial of Service.
**Learning:** Validating matrices and initial points only for finiteness and dimensions is insufficient. In mathematical algorithms, initial states must be strictly validated against the required mathematical domains before entering solver loops.
**Prevention:** In local mathematical algorithms, explicitly evaluate strict mathematical boundaries for initial conditions (e.g. `np.linalg.cholesky` for PD, or testing `np.all(g(x) < 0)`) immediately after initial setup, throwing an actionable `ValueError` before execution reaches main loops or standard matrix operations.
## 2025-10-24 - DoS via Memory Exhaustion (OOM) from Amplified Matrix Allocations
**Vulnerability:** Core mathematical solvers (QP, SQP, Primal-Dual, Interior-Point, BFGS) lazily pre-allocated dense O(N^2) KKT or Hessian matrices based on the sum of dimensions of input vectors (e.g. `n + m`). An attacker providing a long 1D constraint array (e.g., shape `(50000, 1)`, which is small in size) caused the solver to allocate a `(50000, 50000)` dense float64 matrix, instantly consuming ~18GB of RAM and crashing the server with a `MemoryError`.
**Learning:** Validating input array dimensions and shapes is insufficient if the combined derived dimensions used for memory allocation are unbounded. Small payloads can easily trigger disproportionately massive O(N^2) memory allocations.
**Prevention:** Always enforce a hard upper bound on the combined total dimensions (e.g. `n + m <= 10000`) before pre-allocating dense KKT matrices or block systems to prevent memory exhaustion DoS.
